apiVersion: apps/v1
kind: Deployment
metadata:
  name: svd-bridge
  namespace: zaksoft
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svd-bridge
  template:
    metadata:
      labels:
        app: svd-bridge
    spec:
      containers:
      - name: svd-bridge
        image: mock-bridge:v1
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: svd-bridge-service
  namespace: zaksoft
spec:
  selector:
    app: svd-bridge
  ports:
  - port: 7860
    targetPort: 8000
