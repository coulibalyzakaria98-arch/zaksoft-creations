{
  "name": "zaksoft-creations",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "docker:build": "docker compose -f infrastructure/docker/docker-compose.prod.yml build"
  },
  "devDependencies": {
    "turbo": "^1.11.3",
    "prettier": "^3.1.1"
  }
}
